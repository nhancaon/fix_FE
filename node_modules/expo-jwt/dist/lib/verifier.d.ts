import { DecodingOptions, JWTBody } from '../types/jwt';
import { Claims } from '../types/claims';
declare class Verifier {
    body: JWTBody;
    options: DecodingOptions;
    time: number;
    timeSkew: number;
    static verifyAll(body: JWTBody, options: DecodingOptions): void;
    constructor(body: JWTBody, claim: Claims, options?: DecodingOptions);
    verify_exp(): void;
    verify_nbf(): void;
    verify_iat(): void;
    verify_sub(): void;
    verify_iss(): void;
    verify_aud(): void;
}
export default Verifier;
