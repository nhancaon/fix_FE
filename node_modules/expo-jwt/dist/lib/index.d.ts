import { JWTBody, JWTToken, EncodingOptions, DecodingOptions, EncodingKey, JWTDefaultBody } from '../types/jwt';
declare const JWT: {
    encode: (body: JWTBody, key: EncodingKey, options?: EncodingOptions) => string;
    decode: <T = JWTDefaultBody>(token: JWTToken, key: EncodingKey, options?: DecodingOptions) => JWTBody<T>;
};
export default JWT;
